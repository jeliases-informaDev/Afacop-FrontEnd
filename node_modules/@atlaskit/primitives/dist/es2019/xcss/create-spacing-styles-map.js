// eslint-disable-next-line @atlaskit/ui-styling-standard/use-compiled -- Ignored via go/DSP-18766

import { getSerializedStylesMap } from './get-serialized-styles-map';
const spacingProperties = ['padding', 'paddingBlock', 'paddingBlockStart', 'paddingBlockEnd', 'paddingInline', 'paddingInlineStart', 'paddingInlineEnd', 'gap', 'rowGap', 'columnGap'];
export const createSpacingStylesMap = tokenMap => {
  return spacingProperties.reduce((styleMap, spacingProperty) => {
    styleMap[spacingProperty] = getSerializedStylesMap(spacingProperty, tokenMap);
    return styleMap;
  }, {});
};