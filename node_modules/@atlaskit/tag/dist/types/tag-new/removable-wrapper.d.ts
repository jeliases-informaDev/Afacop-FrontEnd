/**
 * Shared utilities for TagNew and AvatarTag components.
 * Note: CSS styles cannot be shared due to Compiled CSS static analysis requirements.
 */
/**
 * @jsxRuntime classic
 * @jsx jsx
 */
import { type ReactNode } from 'react';
import { TagStatus } from './tag-status';
interface RemovableWrapperProps {
    isRemovable: boolean;
    status: TagStatus;
    onShrinkOutExitComplete?: () => void;
    children: ReactNode;
}
export declare function RemovableWrapper({ isRemovable, status, onShrinkOutExitComplete, children, }: RemovableWrapperProps): string | number | boolean | JSX.Element | Iterable<ReactNode> | null | undefined;
export {};
