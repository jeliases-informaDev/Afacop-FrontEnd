/**
 * @jsxRuntime classic
 * @jsx jsx
 */
import { type AriaRole } from 'react';
import { type TagSwatchBeforeTokenName } from './types';
export interface SwatchBeforeProps {
    colorKey: TagSwatchBeforeColorKey;
    /**
     * - `true`: use `color.background.accent.<colorKey>.subtle` from the tag palette
     * - string: a design token *path* resolved with `token()` (accent background tokens are typical)
     */
    swatchBefore?: boolean | TagSwatchBeforeTokenName;
    /**
     * Accessible label for the swatch, paired with `role` to describe the visual meaning
     * (e.g. the hierarchy level name like "Epic").
     */
    swatchBeforeLabel?: string;
    /**
     * The WAI-ARIA role for the swatch element (e.g. `"img"` for informative color indicators).
     */
    swatchBeforeRole?: AriaRole;
}
type TagSwatchBeforeColorKey = 'gray' | 'blue' | 'red' | 'yellow' | 'green' | 'teal' | 'purple' | 'lime' | 'orange' | 'magenta';
/**
 * Leading 12×12px swatch for TagNew / TagDropdownTrigger (before elemBefore).
 */
export default function SwatchBefore({ colorKey, swatchBefore, swatchBeforeLabel, swatchBeforeRole, }: SwatchBeforeProps): JSX.Element | null;
export {};
