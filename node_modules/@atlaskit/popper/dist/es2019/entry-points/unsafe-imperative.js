/**
 * UNSAFE compatibility entry point. Re-exports Popper.js v2 `createPopper` for non-React,
 * imperative consumers moving direct `@popperjs/core` dependency ownership onto `@atlaskit/popper`.
 *
 * Do not use this for new code. It exposes the raw Popper.js engine, which has no top-layer
 * equivalent and is slated to be dropped when `@atlaskit/popper` moves onto `@atlaskit/top-layer`
 * (CSS Anchor Positioning). New overlays should build on `@atlaskit/top-layer` directly.
 */

// eslint-disable-next-line @repo/internal/deprecations/deprecation-ticket-required -- TIERS-3 tracks the @atlaskit/popper Popper.js consolidation and top-layer migration; this entry point is an intentional escape hatch for existing imperative callers, not a public API.
/**
 * @deprecated Escape hatch for existing imperative Popper.js callers only. Do not use for new
 * code — build on `@atlaskit/top-layer` (`Popover` + `useAnchorPosition`) instead. Tracked by
 * TIERS-3.
 */
export { createPopper } from '@popperjs/core';