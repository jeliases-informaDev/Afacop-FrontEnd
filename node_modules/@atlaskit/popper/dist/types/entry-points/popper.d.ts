/**
 * @deprecated Use `import type { State } from '@atlaskit/popper/main'` instead.
 */
export type { State } from '@popperjs/core';
/**
 * @deprecated Use `import { Popper, placements } from '@atlaskit/popper/main'` instead.
 */
export { Popper, placements } from '../popper';
/**
 * @deprecated Use `import type { CustomPopperProps, ManagerProps, Modifier, Placement, PopperArrowProps, PopperChildrenProps, PopperProps, ReferenceProps, StrictModifier, VirtualElement } from '@atlaskit/popper/src/popper'` instead.
 */
export type { CustomPopperProps, ManagerProps, Modifier, Placement, PopperArrowProps, PopperChildrenProps, PopperProps, ReferenceProps, StrictModifier, VirtualElement, } from '../popper';
