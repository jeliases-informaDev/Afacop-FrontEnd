import type { TFocusableFilter } from './types';
/**
 * Returns the last focusable element within the container.
 */
export declare function getLastFocusable({ container, filter, }: {
    container: HTMLElement;
    filter?: TFocusableFilter;
}): HTMLElement | null;
