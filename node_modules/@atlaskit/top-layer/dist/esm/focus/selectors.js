export var selectors = function () {
  // Common exclusion filter applied to every selector.
  var not = ':not([tabindex="-1"]):not([aria-disabled="true"]):not([aria-hidden="true"])';
  var notDisabled = ":not([disabled])".concat(not);
  var focusable = ["a[href]".concat(not), "area[href]".concat(not), "input".concat(notDisabled, ":not([type=\"hidden\"])"), "select".concat(notDisabled), "textarea".concat(notDisabled), "button".concat(notDisabled), "iframe".concat(not), "audio[controls]".concat(not), "video[controls]".concat(not), "[contenteditable]:not([contenteditable=\"false\"])".concat(not), "[tabindex]".concat(notDisabled).concat(not)];
  return {
    focusable: focusable.join(','),
    // Build the `:focus` selector
    focused: focusable.map(function (selector) {
      return "".concat(selector, ":focus");
    }).join(',')
  };
}();