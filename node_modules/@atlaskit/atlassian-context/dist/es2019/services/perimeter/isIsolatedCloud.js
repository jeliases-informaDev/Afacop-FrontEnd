import { ISOLATED_CLOUD_PERIMETERS } from '../../common/constants/isolated-cloud-perimeters';
import { parseAtlCtxCookies } from '../atl-cookies-lookup';
export function isIsolatedCloud() {
  if (typeof document === 'undefined') {
    var _globalThis$ssrContex, _globalThis$ssrContex2;
    // @ts-ignore
    return (_globalThis$ssrContex = (_globalThis$ssrContex2 = globalThis.ssrContext) === null || _globalThis$ssrContex2 === void 0 ? void 0 : _globalThis$ssrContex2.isInIC) !== null && _globalThis$ssrContex !== void 0 ? _globalThis$ssrContex : false;
  }
  const atlCtxCookieValues = parseAtlCtxCookies();
  if (!atlCtxCookieValues) {
    // If the cookies are not set, the current perimeter is non-isolated commercial
    return false;
  }

  // The icDomain check is needed for when federal perimeters are eventually added to Isolated Cloud as well
  return ISOLATED_CLOUD_PERIMETERS.includes(atlCtxCookieValues.perimeter) && atlCtxCookieValues.icDomain !== undefined;
}