import { getDomainInContext } from './getDomainInContext';
export function getUrlForDomainInContext(subdomain, envType) {
  var _globalThis$location$, _globalThis$location;
  const domain = getDomainInContext(subdomain, envType);
  if (!domain) {
    console.warn(`Domain could not be determined for requested subdomain: ${subdomain}`);
    return undefined;
  }
  return `${(_globalThis$location$ = (_globalThis$location = globalThis.location) === null || _globalThis$location === void 0 ? void 0 : _globalThis$location.protocol) !== null && _globalThis$location$ !== void 0 ? _globalThis$location$ : 'https:'}//${domain}`;
}