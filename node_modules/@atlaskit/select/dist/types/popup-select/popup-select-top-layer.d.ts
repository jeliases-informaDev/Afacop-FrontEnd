/**
 * @jsxRuntime classic
 * @jsx jsx
 */
import { type ReactNode } from 'react';
import { type OptionType } from '../types';
import type { PopupSelectProps } from './popup-select';
/**
 * Top-layer implementation of PopupSelect.
 *
 * Replaces the legacy PopupSelect rendering pipeline
 * (react-popper + createPortal + react-focus-lock + @atlaskit/layering + z-index)
 * with the native Popover API + CSS Anchor Positioning via @atlaskit/top-layer.
 *
 * Gated behind the `platform-dst-top-layer` feature flag.
 */
export declare function PopupSelectTopLayer<Option = OptionType, IsMulti extends boolean = false, Modifiers = unknown>({ closeMenuOnSelect, shouldCloseMenuOnTab, footer, searchThreshold, maxMenuWidth, minMenuWidth, maxMenuHeight, target, label, placeholder, testId, isOpen: controlledIsOpen, defaultIsOpen, spacing: _spacing, options, isSearchable, components: consumerComponents, styles: consumerStyles, onChange, onKeyDown, onOpen, onClose, onMenuOpen, onMenuClose, popperProps, shouldPreventEscapePropagation: _shouldPreventEscapePropagation, ...selectProps }: PopupSelectProps<Option, IsMulti, Modifiers>): ReactNode;
