"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isValid = void 0;
var _getDaysInMonth = require("./get-days-in-month");
var isValid = exports.isValid = function isValid(date) {
  var year = date.year,
    month = date.month,
    day = date.day;
  var daysInMonth = (0, _getDaysInMonth.getDaysInMonth)(year, month);
  return 1 <= month && month <= 12 && 1 <= day && day <= daysInMonth;
};