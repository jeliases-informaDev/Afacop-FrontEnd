import _defineProperty from "@babel/runtime/helpers/defineProperty";
import React, { Component } from 'react';
import { fg } from '@atlaskit/platform-feature-flags';
import isModernContextEnabledEnv from '../utils/isModernContextEnabledEnv';
import LegacyAnalyticsContext from './AnalyticsContext/LegacyAnalyticsContext';
import ModernAnalyticsContext from './AnalyticsContext/ModernAnalyticsContext';
/**
 *  @private https://product-fabric.atlassian.net/browse/AFO-815
 *  @deprecated
 */
// eslint-disable-next-line @repo/internal/react/no-class-components
export default class AnalyticsErrorBoundary extends Component {
  constructor(props) {
    super(props);
    // Gated by analytics-next-lock-context-type(-all-afm): keep the value captured at construction so a late gate flip can't swap the context type and remount the subtree.
    _defineProperty(this, "lockedIsModernContext", isModernContextEnabledEnv || fg('analytics-next-use-legacy-context') === false);
    this.state = {
      hasError: false
    };
  }
  componentDidCatch(error, info) {
    const {
      onError
    } = this.props;
    onError && onError(error, info);
    this.setState({
      hasError: true
    });
  }
  render() {
    const {
      data,
      children,
      ErrorComponent
    } = this.props;
    const {
      hasError
    } = this.state;
    const isLockContextTypeAllAfm = fg('analytics-next-lock-context-type-all-afm');
    const isLockContextType = fg('analytics-next-lock-context-type');
    const isModernContext = isLockContextTypeAllAfm || isLockContextType ? this.lockedIsModernContext : isModernContextEnabledEnv || fg('analytics-next-use-legacy-context') === false;
    if (hasError) {
      if (ErrorComponent) {
        if (isModernContext) {
          return /*#__PURE__*/React.createElement(ModernAnalyticsContext, {
            data: data
          }, /*#__PURE__*/React.createElement(ErrorComponent, null));
        }
        return /*#__PURE__*/React.createElement(LegacyAnalyticsContext, {
          data: data
        }, /*#__PURE__*/React.createElement(ErrorComponent, null));
      }
      return null;
    }
    if (isModernContext) {
      return /*#__PURE__*/React.createElement(ModernAnalyticsContext, {
        data: data
      }, children);
    }
    return /*#__PURE__*/React.createElement(LegacyAnalyticsContext, {
      data: data
    }, children);
  }
}